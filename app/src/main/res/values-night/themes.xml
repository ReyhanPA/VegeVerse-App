<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SubmisiAndroidPemula" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/green</item>
    </style>

    <style name="Theme.SubmisiAndroidPemula.Splash" parent="Theme.AppCompat">
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splashscreen</item>
        <item name="android:windowSplashScreenBackground">@color/white</item>
    </style>
</resources>